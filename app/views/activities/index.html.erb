<h2>This is the index page for going to <%= @location %></h2>

<% @activities.each do |a| %>
  <div class = "bg-white shadow border m-3 p-3">
    <h4><%= a.name %></h4>
    <p><%= a.description %></p>
    <p><%= a.price %></p>
  </div>
<% end %>

<%= link_to "Back to home page", root_path %>
